# Bedrock Streamlit Chatbot

A minimal Streamlit chatbot powered by Amazon Bedrock with the Titan Text G1 ‑ Lite model.

## 🚀 Setup

1. Clone this repo.
2. Install dependencies: `pip install -r requirements.txt`
3. Create a `.env` file:
4. Run the app:
```bash
streamlit run app.py
```